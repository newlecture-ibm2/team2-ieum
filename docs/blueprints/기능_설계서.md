# 📋 기능(서비스) 설계서 (Function Design Document)

> **프로젝트**: 지역 축제 통합 정보 플랫폼 (이음)  
> **작성일**: 2026년 3월 27일  
> **작성자**: 이음 팀  
> **버전**: v1.1

---

## 1. 개요

### 1-1. 목적
이 문서는 프로젝트의 모든 API 엔드포인트를 **컨트롤러 단위**로 묶고, 각 컨트롤러가 호출하는 **서비스(UseCase)** 목록을 정리합니다.

### 1-2. 설계 원칙
- **헥사고날 아키텍처**: Controller → UseCase(Port IN) → Service → Port OUT → Adapter
- **공통 응답 포맷**: 모든 API는 `ApiResponse<T>` 형태로 응답
- **인증**: Spring Security + JWT, 비회원/회원/관리자 3단계 권한

### 1-3. API ID 규칙

> **팀 공통 규칙**: `API_{도메인코드}_{4자리 순번}`

| 도메인 코드 | 영역 | 설명 |
|------------|------|------|
| `AUTH` | 인증 | 회원가입, 로그인 |
| `USR` | 사용자 | 프로필 관리 |
| `FES` | 축제 | 축제 조회, 검색, 지도, 달력, 즐겨찾기 |
| `REV` | 리뷰 | 리뷰/별점 CRUD |
| `COM` | 커뮤니티 | 게시글/댓글 CRUD |
| `NTC` | 공지사항 | 공지 조회 |
| `RPT` | 신고 | 신고 접수 |
| `ADM` | 관리자 | 관리자 전용 기능 |

### 1-4. 공통 응답 포맷

```json
// 성공
{ "success": true, "data": { ... }, "error": null }

// 실패
{ "success": false, "data": null, "error": { "code": "AUTH_001", "message": "인증이 필요합니다" } }

// 페이징
{ "success": true, "data": {
    "content": [ ... ],
    "page": 1, "size": 10, "totalElements": 150, "totalPages": 15
  }, "error": null }
```

### 1-5. 공통 헤더

| 헤더 | 값 | 필수 | 설명 |
|------|-----|------|------|
| `Content-Type` | `application/json` | ✅ | 요청/응답 형식 |
| `Authorization` | `Bearer {JWT}` | 🔒 회원/관리자 | 인증 토큰 |

### 1-6. 인증 레벨 범례

| 표기 | 의미 |
|------|------|
| ✅ 비회원 | 인증 없이 접근 가능 |
| 🔒 회원 | JWT 필수 (role=USER) |
| 🔐 관리자 | JWT 필수 (role=ADMIN) |

---

## 2. 전체 API 목록 요약

### 2-1. 축제 영역 (API_FES)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_FES_0001 | GET | `/api/festivals` | 축제 목록 조회 (필터 포함) | ✅ |
| API_FES_0002 | GET | `/api/festivals/{festivalId}` | 축제 상세 정보 조회 | ✅ |
| API_FES_0003 | GET | `/api/festivals/search` | 기간/키워드 기반 축제 검색 | ✅ |
| API_FES_0004 | GET | `/api/festivals/map` | 지도 기반 축제 위치 조회 | ✅ |
| API_FES_0005 | GET | `/api/festivals/calendar` | 캘린더용 일정 데이터 조회 | ✅ |
| API_FES_0006 | POST | `/api/favorites` | 축제 즐겨찾기 추가 | 🔒 |
| API_FES_0007 | DELETE | `/api/favorites/{favoriteId}` | 축제 즐겨찾기 해제 | 🔒 |

### 2-2. 인증 영역 (API_AUTH)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_AUTH_0001 | POST | `/api/auth/signup` | 회원가입 | ✅ |
| API_AUTH_0002 | POST | `/api/auth/login` | 로그인 | ✅ |
| API_AUTH_0003 | POST | `/api/auth/logout` | 로그아웃 | 🔒 |

### 2-3. 사용자 영역 (API_USR)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_USR_0001 | GET | `/api/users/me` | 내 정보 조회 | 🔒 |
| API_USR_0002 | PUT | `/api/users/me` | 내 정보 수정 | 🔒 |
| API_USR_0003 | GET | `/api/users/me/favorites` | 내 즐겨찾기 목록 | 🔒 |
| API_USR_0004 | GET | `/api/users/me/posts` | 내 게시글 목록 | 🔒 |
| API_USR_0005 | GET | `/api/users/me/reviews` | 내 리뷰 목록 | 🔒 |
| API_USR_0006 | GET | `/api/users/me/notifications` | 내 알림 목록 | 🔒 |
| API_USR_0007 | POST | `/api/users/me/fcm-token` | FCM 토큰 등록 | 🔒 |

### 2-4. 리뷰 영역 (API_REV)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_REV_0001 | GET | `/api/reviews` | 리뷰 목록 조회 | ✅ |
| API_REV_0002 | POST | `/api/reviews` | 리뷰 작성 | 🔒 |
| API_REV_0003 | PUT | `/api/reviews/{reviewId}` | 리뷰 수정 | 🔒 |
| API_REV_0004 | DELETE | `/api/reviews/{reviewId}` | 리뷰 삭제 | 🔒 |

### 2-5. 커뮤니티 영역 (API_COM)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_COM_0001 | GET | `/api/community/posts` | 게시글 목록 조회 | ✅ |
| API_COM_0002 | GET | `/api/community/posts/{postId}` | 게시글 상세 조회 | ✅ |
| API_COM_0003 | POST | `/api/community/posts` | 게시글 작성 | 🔒 |
| API_COM_0004 | PUT | `/api/community/posts/{postId}` | 게시글 수정 | 🔒 |
| API_COM_0005 | DELETE | `/api/community/posts/{postId}` | 게시글 삭제 | 🔒 |
| API_COM_0006 | POST | `/api/community/posts/{postId}/comments` | 댓글 작성 | 🔒 |
| API_COM_0007 | DELETE | `/api/community/comments/{commentId}` | 댓글 삭제 | 🔒 |

### 2-6. 공지사항 영역 (API_NTC)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_NTC_0001 | GET | `/api/notices` | 공지사항 목록 조회 | ✅ |
| API_NTC_0002 | GET | `/api/notices/{noticeId}` | 공지사항 상세 조회 | ✅ |

### 2-7. 신고 영역 (API_RPT)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_RPT_0001 | POST | `/api/reports` | 신고 접수 | 🔒 |

### 2-8. 관리자 영역 (API_ADM)

| API ID | Method | Endpoint | 설명 | 인증 |
|--------|--------|----------|------|------|
| API_ADM_0001 | POST | `/api/admin/auth/login` | 관리자 로그인 | ✅ |
| API_ADM_0002 | GET | `/api/admin/festivals` | 관리자용 축제 목록 | 🔐 |
| API_ADM_0003 | POST | `/api/admin/festivals/sync` | 공공 API 수동 동기화 | 🔐 |
| API_ADM_0004 | PATCH | `/api/admin/festivals/{festivalId}/status` | 축제 상태 수동 변경 | 🔐 |
| API_ADM_0005 | GET | `/api/admin/reports` | 신고 목록 조회 | 🔐 |
| API_ADM_0006 | PATCH | `/api/admin/reports/{reportId}` | 신고 처리 | 🔐 |
| API_ADM_0007 | GET | `/api/admin/notices` | 관리자용 공지 목록 | 🔐 |
| API_ADM_0008 | POST | `/api/admin/notices` | 공지 작성 | 🔐 |
| API_ADM_0009 | PUT | `/api/admin/notices/{noticeId}` | 공지 수정 | 🔐 |
| API_ADM_0010 | DELETE | `/api/admin/notices/{noticeId}` | 공지 삭제 | 🔐 |
| API_ADM_0011 | GET | `/api/admin/statistics/regions` | 지역별 축제 분포 통계 | 🔐 |
| API_ADM_0012 | GET | `/api/admin/statistics/activity` | 사용자 활동 통계 | 🔐 |
| API_ADM_0013 | GET | `/api/admin/surveys` | 설문 목록 | 🔐 |
| API_ADM_0014 | POST | `/api/admin/surveys` | 설문 생성 | 🔐 |
| API_ADM_0015 | GET | `/api/admin/surveys/{surveyId}/results` | 설문 결과 | 🔐 |
| API_ADM_0016 | DELETE | `/api/admin/surveys/{surveyId}` | 설문 삭제 | 🔐 |
| API_ADM_0017 | GET | `/api/admin/users` | 사용자 목록 조회 | 🔐 |
| API_ADM_0018 | PATCH | `/api/admin/users/{userId}` | 사용자 상태 변경 | 🔐 |

**총 엔드포인트: 50개**

---

## 3. 축제 영역 (API_FES) — 상세 설계

| 컨트롤러 | 패키지 |
|----------|--------|
| FestivalController | `user.festival.adapter.in.web` |

---

### API_FES_0001: 축제 목록 조회 (필터 포함)

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0001 |
| **엔드포인트** | `GET /api/festivals` |
| **UseCase** | `GetFestivalListUseCase` |
| **Service** | `FestivalQueryService.getFestivalList()` |
| **인증** | ✅ 비회원 |

**Query Parameters:**

| 파라미터 | 타입 | 필수 | 기본값 | 설명 |
|---------|------|------|-------|------|
| page | int | | 1 | 페이지 번호 |
| size | int | | 10 | 페이지 크기 (최대 50) |
| status | string | | | UPCOMING / ONGOING / ENDED |
| areaCode | string | | | 지역 코드 (1=서울, 31=경기 등) |
| category | string | | | 카테고리 코드 |
| sort | string | | latest | latest / popular / rating |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "festivalId": 1, "title": "서울 벚꽃 축제 2026",
        "address": "서울특별시 영등포구 여의서로",
        "startDate": "2026-04-01", "endDate": "2026-04-10",
        "status": "UPCOMING", "imageUrl": "https://...",
        "latitude": 37.5217, "longitude": 126.9219,
        "avgRating": 4.5, "reviewCount": 23, "viewCount": 1520
      }
    ],
    "page": 1, "size": 10, "totalElements": 150, "totalPages": 15
  }
}
```

---

### API_FES_0002: 축제 상세 정보 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0002 |
| **엔드포인트** | `GET /api/festivals/{festivalId}` |
| **UseCase** | `GetFestivalDetailUseCase` |
| **Service** | `FestivalQueryService.getFestivalDetail()` |
| **인증** | ✅ 비회원 |

**Path Parameter:** `festivalId` — 축제 ID (Long)

**Response (200):**
```json
{
  "success": true,
  "data": {
    "festivalId": 1, "title": "서울 벚꽃 축제 2026",
    "description": "여의도 윤중로...", "address": "서울특별시 영등포구 여의서로",
    "startDate": "2026-04-01", "endDate": "2026-04-10",
    "status": "UPCOMING", "imageUrl": "https://...",
    "latitude": 37.5217, "longitude": 126.9219,
    "tel": "02-1234-5678", "category": "A02",
    "areaCode": "1", "source": "API",
    "avgRating": 4.5, "reviewCount": 23, "viewCount": 1521,
    "isFavorite": false
  }
}
```

**비즈니스 규칙:** 조회 시 `view_count` +1 증가 (중복 방지는 세션/쿠키 기반)

**에러:** 404 — 존재하지 않는 축제 ID

---

### API_FES_0003: 기간/키워드 기반 축제 검색

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0003 |
| **엔드포인트** | `GET /api/festivals/search` |
| **UseCase** | `SearchFestivalUseCase` |
| **Service** | `FestivalQueryService.searchFestivals()` |
| **인증** | ✅ 비회원 |

**Query Parameters:**

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| keyword | string | | 검색어 (축제명, 주소 대상) |
| startDate | string | | 시작일 필터 (yyyy-MM-dd) |
| endDate | string | | 종료일 필터 (yyyy-MM-dd) |
| areaCode | string | | 지역 코드 |
| status | string | | UPCOMING / ONGOING / ENDED |
| page | int | | 기본 1 |
| size | int | | 기본 10 |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "festivalId": 1, "title": "서울 벚꽃 축제 2026",
        "address": "서울특별시 영등포구 여의서로",
        "startDate": "2026-04-01", "endDate": "2026-04-10",
        "status": "UPCOMING", "imageUrl": "https://...",
        "avgRating": 4.5, "reviewCount": 23
      }
    ],
    "keyword": "벚꽃",
    "page": 1, "size": 10, "totalElements": 12, "totalPages": 2
  }
}
```

**API_FES_0001과의 차이점:**

| 구분 | API_FES_0001 (목록) | API_FES_0003 (검색) |
|------|-------------------|-------------------|
| 용도 | 메인/목록 페이지 필터링 | 검색바 키워드 + 기간 검색 |
| 키워드 | ❌ | ✅ (축제명, 주소 대상) |
| 날짜범위 | ❌ | ✅ (startDate ~ endDate) |
| 정렬 | sort 파라미터 | 관련도순 (기본) |

---

### API_FES_0004: 지도 기반 축제 위치 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0004 |
| **엔드포인트** | `GET /api/festivals/map` |
| **UseCase** | `SearchFestivalByMapUseCase` |
| **Service** | `FestivalQueryService.searchByMap()` |
| **인증** | ✅ 비회원 |

**Query Parameters:**

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| areaCode | string | | 지역 코드 |
| status | string | | UPCOMING / ONGOING |
| swLat | double | | 지도 남서 위도 (뷰포트 기반) |
| swLng | double | | 지도 남서 경도 |
| neLat | double | | 지도 북동 위도 |
| neLng | double | | 지도 북동 경도 |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "festivals": [
      { "festivalId": 1, "title": "서울 벚꽃 축제", "latitude": 37.5217, "longitude": 126.9219, "status": "UPCOMING", "imageUrl": "..." }
    ],
    "clusters": [
      { "areaCode": "1", "areaName": "서울", "count": 15, "centerLat": 37.5665, "centerLng": 126.978 }
    ]
  }
}
```

---

### API_FES_0005: 캘린더용 일정 데이터 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0005 |
| **엔드포인트** | `GET /api/festivals/calendar` |
| **UseCase** | `GetFestivalByCalendarUseCase` |
| **Service** | `FestivalQueryService.getByCalendar()` |
| **인증** | ✅ 비회원 |

**Query Parameters:**

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| year | int | ✅ | 연도 (2026) |
| month | int | ✅ | 월 (1~12) |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "year": 2026, "month": 4,
    "festivals": [
      { "festivalId": 1, "title": "서울 벚꽃 축제", "startDate": "2026-04-01", "endDate": "2026-04-10", "status": "UPCOMING", "imageUrl": "..." }
    ]
  }
}
```

---

### API_FES_0006: 축제 즐겨찾기 추가

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0006 |
| **엔드포인트** | `POST /api/favorites` |
| **UseCase** | `AddFavoriteUseCase` |
| **Service** | `FavoriteService.addFavorite()` |
| **인증** | 🔒 회원 |

**Request:**
```json
{ "festivalId": 1 }
```

**Response (201):**
```json
{ "success": true, "data": { "favoriteId": 1, "festivalId": 1 } }
```

**에러:** 409 이미 즐겨찾기한 축제 / 404 존재하지 않는 축제

---

### API_FES_0007: 축제 즐겨찾기 해제

| 항목 | 내용 |
|------|------|
| **API ID** | API_FES_0007 |
| **엔드포인트** | `DELETE /api/favorites/{favoriteId}` |
| **UseCase** | `RemoveFavoriteUseCase` |
| **Service** | `FavoriteService.removeFavorite()` |
| **인증** | 🔒 회원 |

**Response (200):**
```json
{ "success": true, "data": { "deleted": true } }
```

---

## 4. 인증 영역 (API_AUTH) — 상세 설계

| 컨트롤러 | 패키지 |
|----------|--------|
| AuthController | `user.auth.adapter.in.web` |

---

### API_AUTH_0001: 회원가입

| 항목 | 내용 |
|------|------|
| **API ID** | API_AUTH_0001 |
| **엔드포인트** | `POST /api/auth/signup` |
| **UseCase** | `RegisterUseCase` |
| **Service** | `AuthService.register()` |
| **인증** | ✅ 불필요 |

**Request Body:**
```json
{
  "email": "user@example.com",
  "password": "Password123!",
  "passwordConfirm": "Password123!",
  "nickname": "축제러버"
}
```

**유효성 검사:**

| 필드 | 규칙 |
|------|------|
| email | 이메일 형식, 중복 불가, 최대 100자 |
| password | 8~20자, 영문+숫자+특수문자 조합 |
| passwordConfirm | password와 일치 |
| nickname | 2~20자, 중복 불가 |

**Response (201):**
```json
{ "success": true, "data": { "userId": 1, "email": "user@example.com", "nickname": "축제러버" } }
```

**에러:** 400 유효성 실패 / 409 이메일·닉네임 중복

---

### API_AUTH_0002: 로그인

| 항목 | 내용 |
|------|------|
| **API ID** | API_AUTH_0002 |
| **엔드포인트** | `POST /api/auth/login` |
| **UseCase** | `LoginUseCase` |
| **Service** | `AuthService.login()` |
| **인증** | ✅ 불필요 |

**Request Body:**
```json
{ "email": "user@example.com", "password": "Password123!" }
```

**Response (200):**
```json
{
  "success": true,
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiJ9...",
    "user": { "userId": 1, "email": "user@example.com", "nickname": "축제러버", "role": "USER", "profileImage": null }
  }
}
```

**에러:** 401 이메일 또는 비밀번호 불일치

---

### API_AUTH_0003: 로그아웃

| 항목 | 내용 |
|------|------|
| **API ID** | API_AUTH_0003 |
| **엔드포인트** | `POST /api/auth/logout` |
| **UseCase** | `LogoutUseCase` |
| **Service** | `AuthService.logout()` |
| **인증** | 🔒 회원 |

**Response (200):**
```json
{ "success": true, "data": { "message": "로그아웃 성공" } }
```

---

## 5. 사용자 영역 (API_USR) — 상세 설계

| 컨트롤러 | 패키지 |
|----------|--------|
| UserController | `user.auth.adapter.in.web` |

---

### API_USR_0001: 내 정보 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0001 |
| **엔드포인트** | `GET /api/users/me` |
| **UseCase** | `GetUserProfileUseCase` |
| **인증** | 🔒 회원 |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "userId": 1, "email": "user@example.com", "nickname": "축제러버",
    "profileImage": "/uploads/profiles/1.jpg", "role": "USER", "createdAt": "2026-03-25T10:00:00"
  }
}
```

### API_USR_0002: 내 정보 수정

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0002 |
| **엔드포인트** | `PUT /api/users/me` |
| **UseCase** | `UpdateUserProfileUseCase` |
| **인증** | 🔒 회원 |
| **Content-Type** | `multipart/form-data` |

**Request (multipart):** nickname, profileImage(파일, 선택)

**Response (200):**
```json
{ "success": true, "data": { "userId": 1, "nickname": "새닉네임", "profileImage": "/uploads/profiles/1_new.jpg" } }
```

**에러:** 409 이미 사용 중인 닉네임

### API_USR_0003: 내 즐겨찾기 목록

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0003 |
| **엔드포인트** | `GET /api/users/me/favorites` |
| **UseCase** | `GetMyFavoritesUseCase` |
| **인증** | 🔒 회원 |

**Query Parameters:** page (기본 0), size (기본 10)

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      { "favoriteId": 1, "festivalId": 10, "title": "서울 벚꽃 축제", "startDate": "2026-04-01", "imageUrl": "..." }
    ],
    "page": 0, "size": 10, "totalElements": 5, "totalPages": 1
  }
}
```

### API_USR_0004: 내 게시글 목록

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0004 |
| **엔드포인트** | `GET /api/users/me/posts` |
| **UseCase** | `GetMyPostsUseCase` |
| **인증** | 🔒 회원 |

**Query Parameters:** page (기본 0), size (기본 10)

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      { "postId": 1, "boardType": "QNA", "title": "주차장 정보 궁금합니다", "viewCount": 45, "commentCount": 3, "createdAt": "2026-04-05T09:30:00" }
    ],
    "page": 0, "size": 10, "totalElements": 12, "totalPages": 2
  }
}
```

### API_USR_0005: 내 리뷰 목록

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0005 |
| **엔드포인트** | `GET /api/users/me/reviews` |
| **UseCase** | `GetMyReviewsUseCase` |
| **인증** | 🔒 회원 |

**Query Parameters:** page (기본 0), size (기본 10)

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      { "reviewId": 1, "festivalId": 10, "festivalTitle": "서울 벚꽃 축제", "rating": 5, "content": "정말 좋았습니다!", "createdAt": "2026-04-11T14:30:00" }
    ],
    "page": 0, "size": 10, "totalElements": 8, "totalPages": 1
  }
}
```

### API_USR_0006: 내 알림 목록

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0006 |
| **엔드포인트** | `GET /api/users/me/notifications` |
| **UseCase** | `GetMyNotificationsUseCase` |
| **인증** | 🔒 회원 |

**Response (200):**
```json
{
  "success": true,
  "data": [
    { "notificationId": 1, "type": "FESTIVAL_START", "message": "즐겨찾기한 '서울 벚꽃 축제'가 내일 시작됩니다!", "isRead": false, "createdAt": "2026-03-31T09:00:00" }
  ]
}
```

### API_USR_0007: FCM 토큰 등록

| 항목 | 내용 |
|------|------|
| **API ID** | API_USR_0007 |
| **엔드포인트** | `POST /api/users/me/fcm-token` |
| **UseCase** | `RegisterFcmTokenUseCase` |
| **인증** | 🔒 회원 |

**Request Body:**
```json
{ "token": "fcm-token-string" }
```

**Response (200):**
```json
{ "success": true, "data": { "message": "FCM 토큰 등록 성공" } }
```

---

## 6. 리뷰 영역 (API_REV) — 상세 설계

| 컨트롤러 | 패키지 |
|----------|--------|
| ReviewController | `user.review.adapter.in.web` |

---

### API_REV_0001: 리뷰 목록 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_REV_0001 |
| **엔드포인트** | `GET /api/reviews` |
| **UseCase** | `GetReviewListUseCase` |
| **인증** | ✅ 비회원 |

**Query Parameters:** festivalId(필수), page, size, sort(latest/rating)

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "reviewId": 1, "userId": 5, "nickname": "축제러버", "profileImage": "...",
        "rating": 5, "content": "정말 멋진 축제였습니다!", "imageUrl": "...",
        "createdAt": "2026-04-11T14:30:00"
      }
    ],
    "avgRating": 4.5, "totalCount": 23,
    "ratingDistribution": { "5": 10, "4": 8, "3": 3, "2": 1, "1": 1 },
    "page": 1, "size": 10, "totalPages": 3
  }
}
```

### API_REV_0002: 리뷰 작성

| 항목 | 내용 |
|------|------|
| **API ID** | API_REV_0002 |
| **엔드포인트** | `POST /api/reviews` |
| **UseCase** | `CreateReviewUseCase` |
| **인증** | 🔒 회원 |
| **Content-Type** | `multipart/form-data` |

**Request:** festivalId, rating(1~5), content(10~500자), image(선택, 5MB)

**비즈니스 규칙:** ENDED 상태 축제만 리뷰 작성 / 축제당 1인 1리뷰

**에러:** 400 유효성 / 403 ENDED 아닌 축제 / 409 이미 리뷰 작성

### API_REV_0003: 리뷰 수정

| 항목 | 내용 |
|------|------|
| **API ID** | API_REV_0003 |
| **엔드포인트** | `PUT /api/reviews/{reviewId}` |
| **UseCase** | `UpdateReviewUseCase` |
| **인증** | 🔒 회원 (본인만) |

**에러:** 403 본인 리뷰 아님 / 404 리뷰 없음

### API_REV_0004: 리뷰 삭제

| 항목 | 내용 |
|------|------|
| **API ID** | API_REV_0004 |
| **엔드포인트** | `DELETE /api/reviews/{reviewId}` |
| **UseCase** | `DeleteReviewUseCase` |
| **인증** | 🔒 회원 (본인만) |

---

## 7. 커뮤니티 영역 (API_COM) — 상세 설계

| 컨트롤러 | 패키지 |
|----------|--------|
| CommunityController | `user.community.adapter.in.web` |

---

### API_COM_0001: 게시글 목록 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0001 |
| **엔드포인트** | `GET /api/community/posts` |
| **UseCase** | `GetPostListUseCase` |
| **인증** | ✅ 비회원 |

**Query Parameters:** boardType(QNA/TIP/FOOD), keyword, page, size

**Response (200):**
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "postId": 1, "boardType": "QNA", "title": "여의도 축제 주차장 정보 궁금합니다",
        "nickname": "축제러버", "viewCount": 45, "commentCount": 3, "createdAt": "2026-04-05T09:30:00"
      }
    ],
    "page": 1, "size": 10, "totalElements": 85, "totalPages": 9
  }
}
```

### API_COM_0002: 게시글 상세 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0002 |
| **엔드포인트** | `GET /api/community/posts/{postId}` |
| **UseCase** | `GetPostDetailUseCase` |
| **인증** | ✅ 비회원 |

**Response:** 게시글 정보 + comments 배열 포함

### API_COM_0003: 게시글 작성

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0003 |
| **엔드포인트** | `POST /api/community/posts` |
| **UseCase** | `CreatePostUseCase` |
| **인증** | 🔒 회원 |
| **Content-Type** | `multipart/form-data` |

**Request:** boardType(QNA/TIP/FOOD), title(2~100자), content(10~5000자), image(선택)

### API_COM_0004: 게시글 수정

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0004 |
| **엔드포인트** | `PUT /api/community/posts/{postId}` |
| **UseCase** | `UpdatePostUseCase` |
| **인증** | 🔒 회원 (본인만) |

### API_COM_0005: 게시글 삭제

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0005 |
| **엔드포인트** | `DELETE /api/community/posts/{postId}` |
| **UseCase** | `DeletePostUseCase` |
| **인증** | 🔒 회원 (본인만) |

**비즈니스 규칙:** 하위 댓글도 함께 삭제 (CASCADE)

### API_COM_0006: 댓글 작성

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0006 |
| **엔드포인트** | `POST /api/community/posts/{postId}/comments` |
| **UseCase** | `CreateCommentUseCase` |
| **인증** | 🔒 회원 |

**Request:** `{ "content": "여의나루역 추천드립니다" }` (2~500자)

### API_COM_0007: 댓글 삭제

| 항목 | 내용 |
|------|------|
| **API ID** | API_COM_0007 |
| **엔드포인트** | `DELETE /api/community/comments/{commentId}` |
| **UseCase** | `DeleteCommentUseCase` |
| **인증** | 🔒 회원 (본인만) |

---

## 8. 공지사항 영역 (API_NTC) — 상세 설계

### API_NTC_0001: 공지사항 목록 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_NTC_0001 |
| **엔드포인트** | `GET /api/notices` |
| **UseCase** | `GetNoticeListUseCase` |
| **인증** | ✅ 비회원 |

### API_NTC_0002: 공지사항 상세 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_NTC_0002 |
| **엔드포인트** | `GET /api/notices/{noticeId}` |
| **UseCase** | `GetNoticeDetailUseCase` |
| **인증** | ✅ 비회원 |

---

## 9. 신고 영역 (API_RPT) — 상세 설계

### API_RPT_0001: 신고 접수

| 항목 | 내용 |
|------|------|
| **API ID** | API_RPT_0001 |
| **엔드포인트** | `POST /api/reports` |
| **UseCase** | `CreateReportUseCase` |
| **인증** | 🔒 회원 |

**Request:**
```json
{ "targetType": "POST", "targetId": 5, "reason": "부적절한 내용이 포함되어 있습니다" }
```

**유효성:** targetType(POST/REVIEW/COMMENT) / targetId 필수 / reason 10~500자

**비즈니스 규칙:** 동일 대상 중복 신고 불가

---

## 10. 관리자 영역 (API_ADM) — 상세 설계

> 모든 관리자 API는 🔐 ADMIN 권한 필수 (API_ADM_0001 제외)

---

### API_ADM_0001: 관리자 로그인

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0001 |
| **엔드포인트** | `POST /api/admin/auth/login` |
| **UseCase** | `AdminLoginUseCase` |
| **인증** | ✅ 불필요 |

**Request/Response:** API_AUTH_0002와 동일 구조 (role="ADMIN")

### API_ADM_0002: 관리자용 축제 목록

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0002 |
| **엔드포인트** | `GET /api/admin/festivals` |
| **UseCase** | `GetFestivalAdminListUseCase` |

API_FES_0001과 유사하나 `source`(API/MANUAL), `sourceId`, `apiModifiedAt` 등 관리 필드 추가

### API_ADM_0003: 공공 API 수동 동기화

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0003 |
| **엔드포인트** | `POST /api/admin/festivals/sync` |
| **UseCase** | `SyncPublicDataUseCase` |

배치 설계서 BAT-003과 동일. 상세는 `배치_설계서.md` 참조.

**Response (200):**
```json
{ "success": true, "data": { "totalProcessed": 350, "inserted": 12, "updated": 8, "skipped": 330, "failed": 0, "executionTime": "00:03:25" } }
```

### API_ADM_0004: 축제 상태 수동 변경

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0004 |
| **엔드포인트** | `PATCH /api/admin/festivals/{festivalId}/status` |
| **UseCase** | `UpdateFestivalStatusUseCase` |

**Request:** `{ "status": "ENDED" }`

### API_ADM_0005: 신고 목록 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0005 |
| **엔드포인트** | `GET /api/admin/reports` |
| **UseCase** | `GetReportListUseCase` |

**Query Parameters:** status(PENDING/RESOLVED), targetType, page, size

### API_ADM_0006: 신고 처리

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0006 |
| **엔드포인트** | `PATCH /api/admin/reports/{reportId}` |
| **UseCase** | `ResolveReportUseCase` |

**Request:** `{ "status": "RESOLVED", "action": "DELETE_TARGET" }`

**action 옵션:** `DELETE_TARGET`(삭제) / `WARN_USER`(경고) / `DISMISS`(기각)

### API_ADM_0007~0010: 공지 CRUD

| API ID | 엔드포인트 | UseCase | 설명 |
|--------|----------|---------|------|
| API_ADM_0007 | `GET /api/admin/notices` | `GetNoticeAdminListUseCase` | 관리자용 공지 목록 |
| API_ADM_0008 | `POST /api/admin/notices` | `CreateNoticeUseCase` | 공지 작성 |
| API_ADM_0009 | `PUT /api/admin/notices/{noticeId}` | `UpdateNoticeUseCase` | 공지 수정 |
| API_ADM_0010 | `DELETE /api/admin/notices/{noticeId}` | `DeleteNoticeUseCase` | 공지 삭제 |

**공지 작성 Request:**
```json
{ "title": "서비스 오픈 안내", "content": "안녕하세요, 이음 서비스가...", "isPopup": true }
```

### API_ADM_0011: 지역별 축제 분포 통계

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0011 |
| **엔드포인트** | `GET /api/admin/statistics/regions` |
| **UseCase** | `GetRegionStatsUseCase` |

**Response (200):**
```json
{
  "success": true,
  "data": {
    "totalFestivals": 350,
    "regions": [
      { "areaCode": "1", "areaName": "서울", "count": 45, "percentage": 12.8 }
    ],
    "statusSummary": { "UPCOMING": 120, "ONGOING": 80, "ENDED": 150 }
  }
}
```

### API_ADM_0012: 사용자 활동 통계

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0012 |
| **엔드포인트** | `GET /api/admin/statistics/activity` |
| **UseCase** | `GetUserActivityUseCase` |

**Query Parameters:** period(WEEK/MONTH/QUARTER), startDate, endDate

### API_ADM_0013~0016: 설문 CRUD

| API ID | 엔드포인트 | UseCase | 설명 |
|--------|----------|---------|------|
| API_ADM_0013 | `GET /api/admin/surveys` | `GetSurveyListUseCase` | 설문 목록 |
| API_ADM_0014 | `POST /api/admin/surveys` | `CreateSurveyUseCase` | 설문 생성 |
| API_ADM_0015 | `GET /api/admin/surveys/{surveyId}/results` | `GetSurveyResultUseCase` | 설문 결과 |
| API_ADM_0016 | `DELETE /api/admin/surveys/{surveyId}` | `DeleteSurveyUseCase` | 설문 삭제 |

### API_ADM_0017: 사용자 목록 조회

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0017 |
| **엔드포인트** | `GET /api/admin/users` |
| **UseCase** | `GetUserListUseCase` |

**Query Parameters:** keyword(이메일/닉네임), role, page, size

### API_ADM_0018: 사용자 상태 변경

| 항목 | 내용 |
|------|------|
| **API ID** | API_ADM_0018 |
| **엔드포인트** | `PATCH /api/admin/users/{userId}` |
| **UseCase** | `UpdateUserStatusUseCase` |

**Request:** `{ "status": "SUSPENDED", "reason": "반복적인 신고 대상" }`

---

## 11. Spring Security URL 패턴 설정

```java
// SecurityConfig.java — URL별 인가 규칙 요약
http.authorizeHttpRequests(auth -> auth
    // ✅ 인증 불필요 (비회원 허용)
    .requestMatchers(HttpMethod.POST, "/api/auth/**").permitAll()
    .requestMatchers(HttpMethod.GET, "/api/festivals/**").permitAll()
    .requestMatchers(HttpMethod.GET, "/api/reviews").permitAll()
    .requestMatchers(HttpMethod.GET, "/api/community/**").permitAll()
    .requestMatchers(HttpMethod.GET, "/api/notices/**").permitAll()
    
    // 🔒 회원 전용
    .requestMatchers("/api/users/me/**").hasRole("USER")
    .requestMatchers(HttpMethod.POST, "/api/reviews").hasRole("USER")
    .requestMatchers(HttpMethod.PUT, "/api/reviews/**").hasRole("USER")
    .requestMatchers(HttpMethod.DELETE, "/api/reviews/**").hasRole("USER")
    .requestMatchers("/api/favorites/**").hasRole("USER")
    .requestMatchers(HttpMethod.POST, "/api/community/**").hasRole("USER")
    .requestMatchers(HttpMethod.PUT, "/api/community/**").hasRole("USER")
    .requestMatchers(HttpMethod.DELETE, "/api/community/**").hasRole("USER")
    .requestMatchers(HttpMethod.POST, "/api/reports").hasRole("USER")
    
    // ✅ 관리자 로그인 — 인증 불필요
    .requestMatchers(HttpMethod.POST, "/api/admin/auth/login").permitAll()
    
    // 🔐 관리자 전용
    .requestMatchers("/api/admin/**").hasRole("ADMIN")
    
    // 🔒 그 외 인증 필요
    .anyRequest().authenticated()
);
```

---

## 12. 에러 코드 표

| 에러 코드 | HTTP Status | 설명 |
|----------|-------------|------|
| `AUTH_001` | 401 | 인증이 필요합니다 |
| `AUTH_002` | 401 | 이메일 또는 비밀번호가 올바르지 않습니다 |
| `AUTH_003` | 403 | 접근 권한이 없습니다 |
| `AUTH_004` | 401 | 토큰이 만료되었습니다 |
| `USER_001` | 409 | 이미 사용 중인 이메일입니다 |
| `USER_002` | 409 | 이미 사용 중인 닉네임입니다 |
| `USER_003` | 404 | 사용자를 찾을 수 없습니다 |
| `FEST_001` | 404 | 축제를 찾을 수 없습니다 |
| `FEST_002` | 502 | 공공 API 호출에 실패했습니다 |
| `FEST_003` | 409 | 이미 동기화가 진행 중입니다 |
| `REVIEW_001` | 404 | 리뷰를 찾을 수 없습니다 |
| `REVIEW_002` | 403 | 종료된 축제만 리뷰를 작성할 수 있습니다 |
| `REVIEW_003` | 409 | 이미 해당 축제에 리뷰를 작성하셨습니다 |
| `REVIEW_004` | 403 | 본인의 리뷰만 수정/삭제할 수 있습니다 |
| `FAV_001` | 409 | 이미 즐겨찾기한 축제입니다 |
| `FAV_002` | 404 | 즐겨찾기를 찾을 수 없습니다 |
| `POST_001` | 404 | 게시글을 찾을 수 없습니다 |
| `POST_002` | 403 | 본인의 게시글만 수정/삭제할 수 있습니다 |
| `COMMENT_001` | 404 | 댓글을 찾을 수 없습니다 |
| `REPORT_001` | 409 | 이미 신고한 대상입니다 |
| `COMMON_001` | 400 | 잘못된 요청입니다 |
| `COMMON_002` | 500 | 서버 내부 오류가 발생했습니다 |

---

> **문서 이력**
>
> | 버전 | 날짜 | 작성자 | 내용 |
> |------|------|-------|------|
> | v1.0 | 2026-03-27 | 이음 팀 | 최초 작성 |
> | v1.1 | 2026-03-27 | 이음 팀 | 팀 규칙 API ID 체계(API_{도메인}_{순번}) 적용, API_FES_0003(검색) 추가 |
> | v1.2 | 2026-03-27 | 이음 팀 | 코드-설계서 동기화: AUTH signup/logout, USR /api/users/me 및 6개 API 추가, SecurityConfig 관리자 로그인 허용 |
