# 로그 사용 가이드

## 1. 개요
프로젝트 규모가 커짐에 따라 코드 내부에 디버깅용 임시 출력문(`console.log`, `System.out.println` 등)이 혼재되어 있습니다. 이는 프로덕션 환경의 로그 모니터링을 방해하고 성능 저하를 유발할 수 있으므로, 명확한 기준을 세워 일관된 로깅 환경을 구축하고자 합니다.

## 2. 현재 문제점
- **프론트엔드**: FCM 로직 및 데이터 패칭 테스트 등 단순 결과 확인용 `console.log`가 방치되어 있음.
- **백엔드**: 비즈니스 로직(예: ReportService) 및 DB 초기화 스크립트에 테스트용 `System.out.println`이 삽입되어 운영 서버 로그 파일에서 추적이 불가능함. 전역 에러 핸들러마저 `ex.printStackTrace()`를 사용해 관리되지 않는 휘발성 에러를 내뿜고 있음.
- **공통**: 디버깅 로그와 운영 필수 장애 추적 로그의 구분이 모호함.

## 3. 프론트엔드 로그 기준
프론트엔드의 화면 단은 클라이언트 브라우저에서 실행되므로 다음 기준을 엄수합니다.

*   **`console.log` / `console.warn`**
    *   **사용 제한**: 개발 중 로컬 디버깅 목적으로만 허용.
    *   **금지 사항**: 커밋 시 반드시 지워서 PR(Pull Request)에 포함되지 않아야 함. (단순 디버깅은 디버거 툴 사용 권장)
*   **`console.error`**
    *   **사용 권장**: `try-catch` 블록 등에서 예외 사항이나 네트워크 장애 상황을 기록할 때 적극 유지.
    *   **목적**: 프로덕션 환경에서 브라우저 디버거를 통한 실제 장애의 역추적을 위한 핵심 단서.

## 4. 백엔드 로그 기준
성능과 운영 모니터링에 직결되므로 가장 엄격히 관리해야 합니다.

*   **`System.out.println` / `System.err.println`**
    *   **단호히 금지 (테스트 코드 제외)**: 표준 출력은 서버 운영 시 로그 파일(File Appender)로 저장되지 않거나, 다른 프레임워크 로그와 섞여 시스템 병목을 유발합니다. 
*   **`ex.printStackTrace()`**
    *   **단호히 금지**: 에러 트래이스 기록을 휘발시키며 추적이 어렵게 만듭니다.
*   **표준 로거 (`Slf4j`)**
    *   **전면 사용**: 모든 클래스의 로깅은 로거(예: `@Slf4j` -> `log.info()`, `log.error()`) 체제로 전환해야 합니다.

## 5. 권장 방식
*   **백엔드 에러 기록**: 
    ```java
    // ❌ 금지: ex.printStackTrace(); 
    // ✅ 권장: log.error("Unhandled Exception Caught: ", ex);
    ```
*   **백엔드 중요 상태 기록**: 
    ```java
    // ❌ 금지: System.out.println("기능 완료 됨");
    // ✅ 권장: log.info("데이터베이스 제약조건 보정 성공");
    ```
*   **프론트엔드 에러 기록 (유지)**:
    ```typescript
    // ✅ 권장:
    catch (err) {
      console.error("FCM 발급 에러:", err); 
    }
    ```

## 6. 기존 코드 처리 정책
광범위한 일괄 치환은 예상치 못한 충돌을 유발할 수 있으므로 아래 방침을 따릅니다.

1.  **각 도메인 담당자 자율 정비 (점진적 제거)**: 본인이 담당하는 파트(Service, 컴포넌트)의 기능을 수정 및 배포할 때, 해당 구역 내의 불필요한 로그들을 함께 지우고 커밋합니다.
2.  **공통/설정 파일 즉시 수정 (우선 조치)**: 전역 에러 핸들러(`GlobalExceptionHandler`)나 DB 초기화 스크립트 등 시스템 코어의 파편화된 출력문은 즉일 내로 `Slf4j` 로 치환 후 배포합니다.
3.  **예외 범위**: `src/test/java` 하위에 위치한 테스트 코드의 출력문(`System.out`)은 별개로 삭제하지 않고 유지합니다.

## 7. 향후 개발 규칙
- **신규 코드 작성 시**: 개발 완료 및 Git Add 시점에 `git diff`를 통해 확인 및 불필요한 디버그 코드를 반드시 제거(또는 로거로 변경)한 채 커밋합니다.
- **코드 리뷰 (PR Review) 체크 포인트**: 
    1. 리뷰어는 등록된 코드에 `System.out`이나 단순히 값만 찍어보는 `console.log`가 포함되어 있으면 즉시 반려(Request Changes) 코멘트를 남깁니다.
    2. 에러 처리가 `ex.printStackTrace()`로 되어있으면 `log.error`로 수정을 권고합니다.


################################################################
[디버깅 로그 검색 가이드 - AI 사용 시]
프로젝트 전체에서 디버깅용 로그를 찾기 위해 아래 프롬프트를 사용합니다.
#################################################################

프로젝트 전체에서 디버깅용 로그를 검색하고 정리해라.

목표:
- console.log, console.warn, console.error
- System.out.println
- printStackTrace

위 로그 사용 위치를 모두 찾아라.

---

작업 요구사항:

1. 전체 검색
아래 패턴을 기준으로 프로젝트 전체를 검색:
- console.log
- console.warn
- console.error
- System.out
- printStackTrace

2. 결과 정리
각 항목을 다음 기준으로 정리:
- 파일 경로
- 라인 번호
- 코드 내용

3. 분류
다음 기준으로 분류:
- 삭제 대상 (console.log, System.out 등 단순 디버깅)
- 유지 대상 (console.error 등 에러 추적)
- 개선 대상 (System.out → logger 전환)

4. 주의사항
- src/test/java 하위는 제외
- 프론트와 백엔드 구분해서 정리
- 추측하지 말고 실제 코드 기준으로 작성

---

출력 형식:
1. 전체 발견 목록
2. 삭제 대상
3. 유지 대상
4. 개선 대상
